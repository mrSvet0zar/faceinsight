# Core deep learning
torch>=2.2.0
torchvision>=0.17.0

# Face detection & landmarks (pre-trained, not re-trained — cf. CLAUDE.md)
# >=1.0: uses the Tasks API (legacy mp.solutions was removed in 1.0)
mediapipe>=1.0

# Image processing
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.24.0,<2.0

# Data handling
pandas>=2.0.0

# Experiment tracking (Phase 2)
wandb>=0.16.0

# Dataset download
kaggle>=1.6.0
python-dotenv>=1.0.0

# Explainability (Phase 3)
grad-cam>=1.5.0

# Backend API (Phase 3)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0

# Tests
pytest>=8.0.0
httpx>=0.27.0
